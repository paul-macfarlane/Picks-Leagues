{
  "name": "@picksleagues/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tooling.json",
    "build": "echo '@picksleagues/api: no build step yet'",
    "dev": "echo '@picksleagues/api: no dev server yet'",
    "test": "echo '@picksleagues/api: no tests yet'",
    "lint": "eslint . --max-warnings 0",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.43.1",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@types/ws": "^8.5.14",
    "dotenv": "^16.5.0",
    "drizzle-kit": "^0.31.1",
    "tsx": "^4.19.4"
  }
}
