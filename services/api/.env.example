# Database connection string. Never commit the real value — .env is gitignored.
#
# Local development (default below): start Docker Postgres with
#   docker compose up -d
# and this connection string works as-is. See services/api/README.md.
#
# Neon (dev/preview/prod): replace with the pooled connection string from the
# Neon dashboard. The driver is chosen automatically from the host — no code
# change needed to switch.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/picksleagues

# Shared secret for protecting /api/cron/* endpoints. Vercel Cron sends this
# in the Authorization: Bearer header automatically. Locally, any non-empty
# value works — generate a strong random string for production.
CRON_SECRET=replace-me-with-a-long-random-string

# Better Auth — JWT signing + cookie session secret. Generate with:
#   openssl rand -base64 32
# Must be at least 32 chars. Must be set in every environment.
BETTER_AUTH_SECRET=replace-me-with-openssl-rand-base64-32

# Better Auth — canonical app origin for OAuth callback URLs and cookie domain.
#
# Resolution precedence (see services/api/src/auth/resolve-auth-base-url.ts):
#   1. Explicit override passed to createAuth() in tests
#   2. BETTER_AUTH_URL env var (wins if set — including the fixed shared preview
#      origin on Vercel Preview environments)
#   3. https://${VERCEL_URL} — Vercel injects VERCEL_URL (host without scheme)
#      into every preview function at runtime; used as a fallback for raw-host
#      non-OAuth verification. OAuth itself always uses BETTER_AUTH_URL (Option B
#      shared preview origin) so the callback URI stays stable.
#   4. Throws with a clear error (local dev and prod require BETTER_AUTH_URL).
#
# Local dev: http://localhost:5173 (Vite proxies to here; session cookie scoped
# to the frontend origin so the SPA can read it — do NOT use :3000 here).
# Production: the canonical prod URL (e.g. https://picksleagues.com).
# Preview: set once in Vercel dashboard as the stable shared preview origin.
BETTER_AUTH_URL=http://localhost:5173

# Google OAuth — create at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: $BETTER_AUTH_URL/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Discord OAuth — create at https://discord.com/developers/applications
# Redirect URI: $BETTER_AUTH_URL/api/auth/callback/discord
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
